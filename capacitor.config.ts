import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'App_Retro_Commerce',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
